{% for post in posts %}
    <h3>{{ post.title }}</h3>
    <h4>{{ post.date | localizeddate('medium', 'none', app.request.locale) }}</h4>
    <a href="{{ path('news_show', {id: post.id}) }}">leggi l'articolo...</a>
{% endfor %}
